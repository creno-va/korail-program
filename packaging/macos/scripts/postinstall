#!/bin/sh
set -eu

APP="/Applications/Korail Analyzer.app"
RUNTIME="$APP/Contents/MacOS/runtime"

if [ -d "$RUNTIME" ]; then
  find "$RUNTIME" -type f \( \
    -name "ffmpeg" -o \
    -name "ffprobe" -o \
    -name "ollama" -o \
    -name "llama-server" \
  \) -exec chmod 755 {} \; 2>/dev/null || true
fi

exit 0
