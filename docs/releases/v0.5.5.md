# v0.5.5

## Changes
- Fix macOS Ollama runtime validation for the official `Ollama.app` resource layout.
- Treat bundled macOS/Linux Ollama as ready when the packaged `ollama` executable exists; Windows still validates `llama-server.exe`.
- Add a regression test for executable-only macOS Ollama runtime bundles.

## Verification
- `scripts/test.cmd`
- `python -m compileall -q src tests`
- `korail-analyzer --version`
- Windows installer build with bundled Ollama and FFmpeg runtime checks
- GitHub Actions macOS installer workflow for arm64 and x86_64 pkg/dmg artifacts
