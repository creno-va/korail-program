# v0.6.1

## Changes
- Bundle the `certifi` CA certificate store in Windows and macOS installers.
- Retry OpenAI API HTTPS requests with the bundled CA store when OS certificate validation fails.
- Add an API settings connection test button that verifies the API key and selected model endpoint before analysis.
- Improve SSL certificate failure messages with guidance for PC time, antivirus HTTPS scanning, company proxy, and custom CA environments.

## Verification
- OpenAI API TLS smoke test without a real key reached the expected `401 Unauthorized` response.
- OpenAI client smoke test with a dummy key reached the expected `401 Incorrect API key` response.
- `python -m compileall -q src tests`
- `scripts/test.cmd`
