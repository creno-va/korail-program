# v0.6.2

## Changes
- Lower the default VQA sampling interval from 30 seconds to 15 seconds.
- Include `low` risk frames in captures and reports by default.
- Tune the frame judge prompt to keep uncertain nearby trackside vegetation as a low-risk watchlist item instead of dropping it as no event.
- Align root video analysis helper scripts with the 15-second / low-risk defaults.
- Update the report header copy to refer to GPT vision VQA.

## Verification
- `python -m compileall -q src tests`
- `scripts/test.cmd`
